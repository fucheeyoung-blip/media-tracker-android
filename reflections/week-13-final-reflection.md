# Final Reflection — ICS 342

**Due:** End of class, Week 13 (August 13, 2026)
**Submitted with:** Your final pull request
**Worth:** 50 points

---

This document is your final written submission for the course. It is not a summary of the syllabus. It is not a list of features you implemented. It is a record of what you actually learned, written by you, about your specific experience building this specific app this semester.

Responses that are vague, generic, or could have been written by someone who wasn't in class will not receive full credit. Responses that were generated by an AI and not substantially revised will be obvious. Write in your own voice. Be specific. Be honest.

There is no page minimum. There is a quality minimum. A response that takes two paragraphs to say something real is worth more than five paragraphs of filler.

---

## Part 1 — Your App

**1a. Open your final pull request on GitHub. Find the commit that you are most proud of — not the largest, not the last one, the one that meant the most to you. Paste the commit URL here and explain why you chose it. What did it take to get there? What was broken before and what worked after?**

> Your answer: 

https://github.com/fucheeyoung-blip/media-tracker-android/pull/10/changes I chose this commit because it's the one where my search function actually started working for real. Before this, SearchScreen was just running against a hardcoded list of results — no matter what you typed into the search bar, you'd get back the same fixed set of items, because nothing was actually querying anything. It looked like it worked in a demo, but it wasn't real. This commit replaced that with an actual call to the API, so search results now come back based on what the user actually types.

---

**1b. Name one screen in your app that you think is genuinely well-built. Not perfect — well-built. Explain specifically why: what design decisions did you make, what did you refactor, and how does it differ from how you would have approached it in week 2?**

> Your answer:

The screen I think is genuinely well-built is my create account screen. It validates required fields, checks that the password and confirm password fields match, and checks that the email is in a valid format before letting the user submit and when something fails, it shows a specific error message (not a generic "something went wrong") so the user actually knows what to fix.

---

**1c. Name one screen or feature that you are not satisfied with. What is wrong with it? If you had one more week, what specifically would you change?**

> Your answer:

The screen I am not satisfied with is probably my "Popular This Week" section on my Search screen. My actual search bar works fine now and pulls real results from the API, but that section still just shows the same three hardcoded items every time (Dune, Inception, Severance) no matter what it's not connected to anything real. It looks fine at first glance so it's easy to miss, but it's fake data pretending to be live. 

---

## Part 2 — A Specific Bug

**2a. Describe the hardest bug you fixed this semester. Not the most recent one — the one that took the longest or cost you the most confusion. What was the symptom? What did you think the problem was at first? What was it actually? How did you find it?**

> Your answer:

I think the hardest bug for me was on the PUT /priorities endpoint. I kept getting the same error back "Missing required field: mediaId" no matter what I tried. At first I thought I just had the wrong field name or a typo somewhere. I tried sending it as a wrapped object, then as a plain array, and got the exact same error both times, which made it feel like nothing I changed actually mattered.
Turns out the endpoint only takes one priority item at a time, not a list but my models were built assuming it took a list, since that's how a normal bulk-update endpoint usually works. 
I only figured that out by actually reading the raw request/response bytes in Logcat instead of guessing. Once I saw that, I knew what to fix I had to rewrite my code to loop through 
each item and call the endpoint once per item instead of sending them all at once.

---

**2b. Copy and paste the specific lines of code you changed to fix it. (This can be a before/after comparison, a diff, or just the relevant snippet.) Explain in plain English what the fix does and why it works.**

> Your answer (include code):

This is the before

suspend fun updatePriorities(priorities: List<Priority>): List<Priority> {
val response = api.updatePriorities(UpdatePrioritiesRequest(priorities))
if (response.isSuccessful) {
return (response.body() ?: emptyList()).sortedBy { it.orderIndex }
}
throw Exception("Something went wrong (${response.code()}).")
}

than the after
suspend fun updatePriorities(priorities: List<Priority>): List<Priority> {
val results = mutableListOf<Priority>()
for (p in priorities) {
val writeItem = PriorityWriteItem(
mediaId = p.mediaId,
priority = p.priority,
orderIndex = p.orderIndex,
estimatedTimeHours = p.estimatedTimeHours,
notes = p.notes,
)
val response = api.updatePriorities(writeItem)
if (response.isSuccessful) {
response.body()?.let { results.add(it) }
} else {
throw Exception("Something went wrong (${response.code()}).")
}
}
return if (results.size == priorities.size) results.sortedBy { it.orderIndex }
else priorities.sortedBy { it.orderIndex }
}

---

## Part 3 — What You Actually Learned

**3a. Pick the concept from this semester that took the longest to actually understand — not just to implement, but to understand. Describe what you thought it was before you understood it, what changed, and how you would explain it now to a student who was exactly where you were at the start of the semester.**

> Your answer:

The thing that took me longest to understand was why UserRepository exists between the ViewModel and the API. I knew the mechanics: the ViewModel calls the repository, which calls the API, but not why, beyond "separation of concerns." It clicked once I reused the same repository across more than one ViewModel and saw that the ViewModel doesn't need to know how data is fetched, it just asks for it. I'd tell a week-1 student: it's not boilerplate, it's the one place that knows how to get or change a piece of data so every ViewModel can just ask for it.

---

**3b. Your weekly reflections had a "Still Confused" section. Look back at your early reflections — weeks 1 through 4. Find something you wrote that you were confused about then. Are you still confused about it? If not, when and how did it click? If you still are, say so honestly and describe what the sticking point is.**

(If you can't access your early reflections, describe something you remember being confused about in the first half of the semester.)

> Your answer:

In my week 3 reflection I said I didn't understand why UserRepository needed to be its own layer instead of just calling the API from the ViewModel. I'm not confused about that anymore it clicked when I reused the same repository across more than one ViewModel later in the semester. Actually seeing that reuse happen made it click in a way reading about "separation of concerns" hadn't.

---

**3c. Name one thing a pod mate said, asked, or showed you during a code review or work session that changed how you approached something. It doesn't have to be a big thing. What was it, and what did it change?**

> Your answer:

Fasika left a comment on my DefaultMediaDetailRepository.kt, on the line private val api = RetrofitInstance.mediaApiService(sessionRepository), saying a generic name like api could cause confusion for future maintenance. It was small, but it changed how I name things I started being more specific (like mediaApi) instead of just calling everything api.

---

## Part 4 — Your Bonus Feature

**4a. Describe your bonus feature in one paragraph as if you were explaining it to someone who has never used an app before. What does it do? Why would a user want it?**

> Your answer:

My bonus feature is called Priorities. It lets a user take something from their "Want To list" like a book they haven't started or a show they haven't watched yet and mark it as a priority, with a High, Medium, or Low label.

---

**4b. What was the technically hardest part of building it? Name a specific function, flow, or data structure that gave you trouble, and explain what the problem was.**

> Your answer:

I think the hardest part of building it was getting the filter chips (All/High/Medium/Low) to actually filter the list right. 
The chip would show as selected, but the list underneath wouldn't always match it'd show the wrong items or not update right away. 
I think the problem was that the selected chip and the actual filtered list weren't properly tied together, so they could get out of sync. 
Fixing it meant making the filter selection drive the list directly instead of tracking them separately.

---

**4c. Your bonus feature has tests. Open the test file and paste the test you think is most valuable — the one that would catch the most important failure. Explain what it proves and what it does not prove.**

> Your answer (include code):

I do not see any test file for my bonus feature.

---

## Part 5 — Looking Forward

**5a. If you were going to continue developing this app after the semester ends, what would you build next and why?**

> Your answer:

If I kept working on this app, the first thing I'd fix is the "Popular This Week" section on the Search screen, since it's still just hardcoded data pretending to be real. I'd either hook it up to an actual trending/popular endpoint if the API has one, or replace it with something honest and useful, like a "Recent Searches" section built from real data. Right now it's the one part of the app that looks finished but isn't, and that bugs me more than the stuff that's obviously unfinished.

---

**5b. A friend tells you they want to learn Android development. Based specifically on your experience this semester — not what you've read, what you lived — what is the one thing you would tell them to understand before they write a single line of code?**

> Your answer:

I would tell them to be careful with search features specifically — it's easy to build one that looks like it works because something shows up on screen, but it's secretly hardcoded and not actually querying anything. 
Then I would also tell them to make sure their api is working for the searching of the books/shows/movies, not just assuming it works because something appears.

---

## Grading

| Section | Points |
|:---|:---:|
| Part 1 — Your App | 12 |
| Part 2 — A Specific Bug | 12 |
| Part 3 — What You Actually Learned | 14 |
| Part 4 — Your Bonus Feature | 8 |
| Part 5 — Looking Forward | 4 |
| **Total** | **50** |

Full credit in each section requires specificity — code, names, dates, conversations, actual things that happened. Half credit for responses that are genuine but stay at the surface. No credit for responses that are generic, missing, or could have been written by someone who was not in this class.
